<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#FFFFF0] h-screen flex items-center justify-center">

  <div class="bg-white p-6 rounded-lg shadow-lg w-80">
    <h2 class="text-2xl font-bold text-center mb-4">Forgot Password</h2>

    <% if(error){ %>
      <p class="text-red-500 mb-2"><%= error %></p>
    <% } %>

    <% if(message){ %>
      <p class="text-green-600 mb-2"><%= message %></p>
    <% } %>

    <form action="/forgot-password" method="POST" class="flex flex-col gap-3">
      <input type="email" name="email" class="border p-2 rounded outline-none" placeholder="Enter your email" required>

      <button class="bg-blue-500 text-white py-2 rounded hover:scale-105 transition">
        Send Reset Link
      </button>
    </form>

    <p class="text-center mt-3 text-blue-700">
      <a href="/access">Back to Login</a>
    </p>
  </div>

</body>
</html>
